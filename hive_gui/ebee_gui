PPinOutBee:
    param: variable (ref)

    in/out:
        trigger: "do push/pull"

        value


Non IO
Triggerfunc:
    param: code - callable (optional)
    output: trigger


Triggerable:
    param: code - callable (required)
    input: trigger

Attribute:
    data_type
        [then] -> start_value

IO:
Antenna:
    output: input


Output:
    input: output


Hook:
    input: trigger


Entry:
    output: trigger


TODO: Add Hive previewer with node constructed of IO pins

################################### HELPERS
Add a trigger "helper" UI node

    Pull In:
        Can trigger
        Can be triggered {Use node link}

    Push Out:
        Can trigger
        Can be triggered {Use node link}

    Push In:
        Can trigger

    Pull Out:
        Can trigger


TriggerHelper:
    input:
        push/pull in (mimic, linked)

    outputs:
        push/pull out (mimic, linked)

        pretrigger
        trigger


OutputTriggerHelper:
    outputs:
        push/pull out (mimic, linked)

    inputs:
        push/pull in (mimic, linked)

        pretrigger
        trigger


Hop in / out

##############################

Dragging from push connection to trigger generates a trigger connection
Add version to hivemap (nest hivemap inside spyder version, using generic string)

PPINOUT bees take data type of attribute used









------------------Changes------------------------
Socket:
    hover:
        show:
            index of each connection [editable]
            if trigger:
                pre/post of connections [editable]

    click:
        show and keep hover info


#TODO:
#Mimicking of data type and mode (or lower level color because data type isn't important, as long as conn allowed)
#Add param GUI for bees (instead of hive GUI)
#Bees ex/i distinction! - allow flag in GUI, so need exporter to export BeeNode.external? Force no for some bees (antenna, ppin)




# BEGIN DONE
Bees: different colour to Hive nodes

Add connection tracking to UI:
    Remove pin.targets
    Add on_connected, on_disconnected method to increment pin counter

Connection info box

When importing / exporting, sort connections for same start pins by index (only needed for push)!

Connection info:
    start pin
    end pin
    index = -1

Use + / - to change index. (resort pin list (order matters ONLY on output pin))
# END DONE